package com.nextp.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * NextP Clay System — paleta oficial.
 * Ver docs/05-design-system.md
 */
object NextPColors {
    // Principais
    val PrimaryBlue = Color(0xFF006DFF)
    val LightBlue = Color(0xFF72D7FF)
    val Background = Color(0xFFF7FBFF)
    val White = Color(0xFFFFFFFF)

    // Texto
    val TextDark = Color(0xFF101828)
    val TextMuted = Color(0xFF667085)

    // Semânticas
    val Success = Color(0xFF12B76A)
    val Warning = Color(0xFFF79009)
    val Danger = Color(0xFFF04438)

    // Secundárias / categorias
    val SoftPurple = Color(0xFF9B7EDE)
    val SoftPink = Color(0xFFFF7A9A)
    val CoinYellow = Color(0xFFFDB022)

    // Superfícies clay
    val CardWhite = Color(0xFFFFFFFF)
    val CardBlueSoft = Color(0xFFEEF6FF)
    val ShadowBlue = Color(0x33006DFF)

    // Dark theme
    val DarkBackground = Color(0xFF0B1220)
    val DarkSurface = Color(0xFF141C2E)
    val DarkTextDark = Color(0xFFF2F6FF)
    val DarkTextMuted = Color(0xFF98A2B3)
}
